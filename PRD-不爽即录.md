# 不爽即录 — 产品需求文档（PRD）

| 项目 | 内容 |
|------|------|
| 文档版本 | V1.0 |
| 撰写日期 | 2026-04-08 |
| 产品名称 | 不爽即录 |
| 参考原型 | `不爽即录.html` |
| 原始需求 | 《系统不爽即录.docx》 |

---

## 1. 背景与目标

### 1.1 业务背景

跨境电商 ERP 日常使用中，运营、产品、广告等角色频繁遇到「差点意思」的体验问题：筛选要重复选、批量入口难找、页面加载慢、数据状态不同步等。这类「小不爽」往往不会转化为正式需求——业务觉得「提了也没用、写需求太麻烦」，产品侧也缺少真实体验断点数据，迭代方向容易依赖主观判断。

### 1.2 产品目标

在 ERP 各业务页面嵌入轻量反馈入口，将反馈成本降低到 **5 秒内一次提交**；由系统自动采集上下文，经 AI 完成分类、去重聚合与优先级排序，输出产品经理可直接使用的 **不爽即录看板**，形成「收集 → 分派 → 处理 → 闭环」链路。

### 1.3 成功指标（建议）

| 指标 | 目标 |
|------|------|
| 单次反馈完成时长 | ≤ 5 秒（选标签 + 提交） |
| 隐藏需求发现量 | 较现状提升 3～5 倍（上线后 3 个月观测） |
| 产品整理反馈耗时 | 从约 2 小时/日 → 约 20 分钟/日 |
| P0 问题响应时效 | 24 小时内有人跟进 |

---

## 2. 用户角色

| 角色 | 使用场景 | 核心诉求 |
|------|----------|----------|
| 业务人员（运营/产品/广告等） | 在 ERP 任意页面遇到体验问题 | 快速反馈、不被打断、知道有人跟进 |
| 产品经理 | 不爽即录看板 | 看趋势、筛问题、分优先级、转需求 |
| 模块负责人 / 处理人 | 看板详情、日志 | 认领问题、更新状态、填写处理意见 |
| 系统管理员 | 配置处理人 | 按产品中心/营销中心配置默认处理人 |

---

## 3. 产品范围

### 3.1 本期范围（对齐当前原型）

- ERP 页面嵌入式「点这里说不爽」反馈入口
- 反馈登记弹框（模块、快捷标签、描述、图片）
- 不爽即录看板（KPI、筛选、列表、分页、趋势图）
- 反馈详情与处理（状态、意见、日志）
- 处理人配置（产品中心 / 营销中心）
- 数据导出（CSV）
- 钉钉工作通知（新反馈、指定处理人、处理结果回访）

### 3.2 不在本期范围（后续版本）

- 对接公司统一需求池自动建单
- AI 根因分析对接真实日志平台
- 业务侧「我的反馈」个人中心
- 每日 5 次防刷（需求文档有、原型未实现，正式版需补）

---

## 4. 信息架构

```
ERP 业务页面
  └── 点这里说不爽（FAB）
        └── 反馈登记弹框

侧栏菜单
  ├── listing信息变动通知（示例 ERP 页，演示嵌入场景）
  └── 不爽即录（产品看板）
        ├── KPI 概览
        ├── 筛选 + 列表 + 分页
        ├── 配置处理人
        ├── 导出
        └── 近 7 日反馈趋势

弹框
  ├── 反馈详情（处理信息 + 登记信息）
  ├── 操作日志
  ├── 配置处理人
  └── 图片预览 Lightbox
```

---

## 5. 功能需求

### 5.1 反馈入口（FAB）

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F-001 | 在 ERP 业务页面右下角展示悬浮按钮「点这里说不爽」 | P0 |
| F-002 | 支持快捷键 `Ctrl + Shift + B` 打开反馈弹框 | P1 |
| F-003 | 进入「不爽即录」看板页时隐藏 FAB，避免重复入口 | P1 |
| F-004 | Hover 时按钮视觉强化（颜色/阴影变化），提示可点击 | P2 |

**交互说明：**
- 默认展示于 listing 等业务示例页
- 看板页不展示 FAB

---

### 5.2 反馈登记弹框

#### 5.2.1 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| 所属模块 | 是 | 三级级联选择，支持选一级/二级/三级；支持搜索；默认「营销中心 / listing管理 / new listing信息变动通知」 |
| 快捷反馈 | 是 | 五选一快捷标签 |
| 详细描述 | 否 | 文本，最多 500 字 |
| 问题图片 | 否 | 最多 5 张；单张 ≤ 5MB；支持 JPG/PNG/GIF/WebP/BMP |

#### 5.2.2 快捷标签与 AI 分类映射

| 快捷标签 | 内部分类 | 默认优先级 |
|----------|----------|------------|
| 😞 太慢了 | 性能类 | P1 |
| 🤔 逻辑错了 | 逻辑错误类 | P0 |
| 🔧 缺功能 | 功能缺失类 | P1 |
| 😵‍💫 不好用 | UI/UX 类 | P2 |
| 📝 其它 | 其它类 | P2 |

> **正式版：** 在快捷标签基础上，结合用户填写的「详细描述」做语义理解，生成摘要与改进建议。  
> **原型：** 按标签规则映射分类/优先级，摘要用描述或标签默认文案。

#### 5.2.3 模块选择器

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F-010 | 三级级联：产品中心、营销中心及下属完整菜单 | P0 |
| F-011 | 点击**名称**选中该级并关闭；点击 **›** 仅展开下级 | P0 |
| F-012 | 支持输入关键字搜索模块路径 | P1 |
| F-013 | 面板底部提示：「点击名称选中本级，点击 › 展开下级」 | P2 |

#### 5.2.4 提交与关闭

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F-020 | 未选模块或未选快捷标签时不可提交，并 Toast 提示 | P0 |
| F-021 | 提交成功后关闭弹框，Toast「已提交」 | P0 |
| F-022 | 点击遮罩 / × / 取消可关闭；有未保存内容时二次确认 | P0 |
| F-023 | 提交时自动记录：反馈人（当前登录用户）、反馈时间、页面上下文 | P0 |

#### 5.2.5 自动采集上下文（正式版）

提交时后台自动附加（用户无需填写）：

- 当前页面 URL、所属模块
- 最近 3 次页面元素点击轨迹
- 页面停留时长
- 浏览器 / 操作系统 / 屏幕分辨率
- 前端性能日志摘要、接口耗时（脱敏）

---

### 5.3 不爽即录看板

#### 5.3.1 KPI 指标

| KPI | 统计口径 |
|-----|----------|
| 今日反馈 | 当日原始反馈条数 |
| P0 待关注 | 当前筛选结果中优先级为 P0 的聚合条数 |
| 待处理 | 处理状态 = 待处理 |
| 处理中 | 处理状态 = 处理中 |
| 已处理 | 处理状态 = 已处理 / 已转需求 / 无需处理 |

#### 5.3.2 筛选区

筛选项样式与 listing 等业务页筛选区一致（260×34px）。

| 筛选项 | 类型 | 说明 |
|--------|------|------|
| 分类 | 下拉 | 性能类 / 功能缺失类 / 逻辑错误类 / UI/UX 类 / 其它类 |
| 优先级 | 下拉 | P0 / P1 / P2 |
| 反馈人 | 文本 | 模糊匹配反馈人 |
| 处理状态 | 下拉 | 待处理 / 处理中 / 已处理 / 已转需求 / 无需处理 |
| 模块 | 级联 | 与登记弹框同源模块树 |

筛选变更后列表回到第 1 页。

#### 5.3.3 列表字段

| 列名 | 说明 |
|------|------|
| 勾选 | 支持全选/行选，选中行高亮 |
| 模块 | 反馈所属模块路径 |
| 分类 | AI 分类标签 |
| 优先级 | P0 / P1 / P2 |
| 反馈人 | 同聚合下多人去重展示，超过 3 人显示「等 N 人」 |
| 详细描述 | 用户描述；无则展示快捷标签 |
| 问题图片 | 缩略图，最多展示 5 张，可点击放大 |
| 反馈时间 | 聚合内最新一条反馈时间 |
| 处理人 | 已处理：实际处理人；未处理：按模块配置的处理人 |
| 处理状态 | 只读标签 |
| 操作 | 详情、日志 |

#### 5.3.4 分页

- 展示：共 N 条、每页条数（10/20/30/50/100）、页码、前往 X 页
- 默认 30 条/页

#### 5.3.5 工具栏

| 按钮 | 说明 |
|------|------|
| 配置处理人 | 蓝色主按钮，打开处理人配置弹框 |
| 导出 | 蓝色主按钮，导出当前聚合列表 CSV |

#### 5.3.6 近 7 日反馈趋势

- 柱状图展示近 7 天原始反馈条数
- 位于列表右侧卡片区域

---

### 5.4 AI 聚合规则

#### 5.4.1 聚合维度（本期）

相同以下字段的多条原始反馈合并为一条聚合记录：

```
所属模块 + 分类 + 快捷标签 + 摘要前 30 字
```

#### 5.4.2 优先级

- 聚合内取最高优先级（P0 > P1 > P2）
- 逻辑错误类默认 P0

#### 5.4.3 正式版增强（规划）

- 语义相似度去重（不仅限摘要前 30 字）
- 热度 = 反馈次数 × 页面使用频率 × 业务权重
- 自动生成改进建议与预估人日

---

### 5.5 反馈详情

#### 5.5.1 处理信息（可编辑）

| 字段 | 说明 |
|------|------|
| 处理状态 | 待处理 / 处理中 / 已处理 / 已转需求 / 无需处理 |
| 处理意见 | 文本，最多 500 字 |

- 修改后须点击「保存」才生效，不即时写入
- 关闭时有未保存修改需二次确认

#### 5.5.2 登记信息（只读）

- 所属模块、快捷标签、详细描述、问题图片
- 图片支持 Lightbox 放大、左右切换

#### 5.5.3 状态变更副作用

- 保存时记录**实际处理人**为当前登录用户
- 写入操作日志：状态变更、意见更新
- 当处理状态变更为 **已处理 / 已转需求 / 无需处理** 时，向反馈人发送钉钉通知

---

### 5.6 操作日志

| 列 | 说明 |
|----|------|
| 操作人 | 执行操作的用户 |
| 操作内容 | 如「提交不爽反馈」「处理状态变更：待处理 → 处理中」「钉钉通知（新反馈待处理）→ 张伟、陈静」 |
| 操作时间 | YYYY-MM-DD HH:mm:ss |

---

### 5.7 配置处理人

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F-070 | 按**产品中心、营销中心**两个大类配置处理人 | P0 |
| F-071 | 多人用顿号或逗号分隔，如「张三、李四」 | P0 |
| F-072 | 保存至系统配置；看板「处理人」列未处理记录展示配置值 | P0 |
| F-073 | 模块路径按一级大类匹配（如「营销中心 / listing管理 / …」→ 营销中心） | P0 |
| F-074 | 批量「修改处理人」时，向**新增**的处理人发送钉钉通知 | P0 |

---

### 5.8 导出

- 格式：CSV（UTF-8 BOM，Excel 可直接打开）
- 文件名：`不爽即录聚合导出_YYYY-MM-DD.csv`
- 字段：模块、分类、优先级、反馈人、详细描述、问题图片数、反馈时间、处理人、处理状态、处理意见

---

### 5.9 钉钉通知

通过钉钉工作通知（或群机器人 @ 指定人）触达相关角色。正式版对接钉钉开放平台；原型以 Toast + 控制台日志模拟发送，并在操作日志中记录通知对象。

#### 5.9.1 触发规则

| 触发时机 | 通知对象 | 说明 |
|----------|----------|------|
| 用户提交新反馈 | 该模块配置的默认处理人 | 按一级大类匹配「配置处理人」 |
| 看板批量「修改处理人」 | 新增的处理人 | 相对原处理人（含默认配置） diff 出新增姓名；仅通知新增者 |
| 详情保存且状态为已处理 / 已转需求 / 无需处理 | 反馈人 | 仅状态**发生变更**时触发；含处理意见与处理人 |

#### 5.9.2 通知示例

**示例 1：新反馈待处理（→ 默认配置处理人）**

```
【不爽即录】新反馈待处理

模块：营销中心 / listing 管理 / 信息变动通知
分类：性能类 | 优先级：P1
反馈人：运营-小周
描述：页面加载太慢，筛选后要等好几秒才出结果
请及时登录「不爽即录看板」处理。
```

**示例 2：指定处理人（→ 修改处理人时新增的处理人）**

```
【不爽即录】您被指定为处理人

模块：产品中心 / 产品管理 / SKU 管理
分类：功能缺失类 | 优先级：P2
反馈人：运营-小李
描述：批量修改分类的入口找不到，要在三个页面来回切
操作人：产品经理
请登录「不爽即录看板」查看详情并跟进。
```

**示例 3：反馈处理结果（→ 反馈人，状态为已处理 / 已转需求 / 无需处理）**

```
【不爽即录】您的反馈已有处理结果

模块：营销中心 / listing 管理 / 信息变动通知
您的反馈：待处理状态筛选后条数和实际不一致
处理状态：已处理
处理意见：已修复筛选逻辑，今晚发版后生效
处理人：张伟
感谢您的反馈！
```

#### 5.9.3 正式版对接说明

- 接收人需映射为钉钉 userId / 手机号
- 多人配置时用顿号、逗号分隔，逐个 @ 或批量发送
- 消息体附带看板详情链接（需 SSO 或内网地址）
- 发送失败需重试并记录失败日志

---

## 6. 数据模型（核心字段）

### 6.1 原始反馈（Feedback）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一 ID，如 FB-{timestamp} |
| userId | string | 反馈人 |
| tag | string | slow / logic / missing / ux / other |
| quickLabel | string | 快捷标签展示名 |
| text | string | 详细描述 |
| summary | string | AI 摘要 |
| category | string | 分类 |
| priority | string | P0/P1/P2 |
| suggestion | string | AI 改进建议 |
| module | string | 模块路径 |
| images | array | 图片列表 { id, name, type, dataUrl } |
| context | object | 页面上下文 |
| status | string | 处理状态 |
| processHandler | string | 实际处理人 |
| processOpinion | string | 处理意见 |
| logs | array | 操作日志 |
| createdAt | string | 反馈时间 |

### 6.2 处理人配置（HandlerConfig）

```json
{
  "产品中心": "李明、王芳",
  "营销中心": "张伟、陈静"
}
```

---

## 7. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 反馈弹框打开 ≤ 300ms；看板列表首屏 ≤ 2s（正式版，万级数据需服务端分页） |
| 安全 | 不上传商品价格、广告出价等敏感业务数据；图片与日志脱敏 |
| 兼容 | Chrome / Edge 最新两个大版本；分辨率 ≥ 1366×768 |
| 防滥用 | 每用户每日最多 5 次反馈（正式版） |
| 可用性 | 核心路径不超过 3 次点击完成反馈 |

---

## 8. 界面与交互规范

- 视觉对齐 ERP 现有风格（顶栏、侧栏、筛选区、表格卡片、Element 风格分页）
- 主色：`#409eff`；危险色：`#f56c6c`；成功色：`#67c23a`
- 反馈弹框：分区布局（模块 / 快捷反馈 / 描述 / 图片），快捷标签选中显示 ✓
- 处理状态色：待处理灰、处理中蓝、已处理绿、已转需求橙、无需处理浅灰

---

## 9. 版本规划

| 阶段 | 周期 | 交付内容 |
|------|------|----------|
| **MVP** | 2 周 | FAB + 反馈表单 + 后台落库 + 基础 AI 分类 + 简单列表 |
| **P1（当前原型对齐）** | +2 周 | 上下文采集、去重聚合、看板 KPI/筛选/分页、详情处理、处理人配置、导出 |
| **P2** | +1 月 | AI 根因分析、钉钉回访、需求池对接、防刷与权限 |

---

## 10. 验收标准

### 10.1 业务侧反馈

- [ ] 在 listing 示例页可看到 FAB，点击后 5 秒内完成一次反馈提交
- [ ] 必选模块与快捷标签校验生效
- [ ] 可上传图片且限制（5 张 / 5MB）生效
- [ ] 有编辑内容时关闭弹框出现确认提示

### 10.2 看板侧

- [ ] KPI 与列表、筛选联动正确
- [ ] 五类分类、五种处理状态筛选正确
- [ ] 分页、跳转、每页条数切换正常
- [ ] 详情修改状态/意见后保存，列表与日志同步更新
- [ ] 配置处理人后，未处理记录展示对应处理人
- [ ] 新反馈、修改处理人、状态闭环时钉钉通知触发正确（原型可控制台验证）
- [ ] 导出 CSV 字段完整、中文不乱码

### 10.3 聚合

- [ ] 相同模块+分类+标签+摘要前缀的多条反馈合并为一条
- [ ] 逻辑错误类聚合优先级为 P0

---

## 11. 原型与正式版差异说明

| 能力 | 原型实现 | 正式版目标 |
|------|----------|------------|
| 数据存储 | 浏览器 localStorage + 内置演示数据 | 服务端数据库 |
| AI 分类 | 快捷标签规则映射 | LLM 语义分类 + 摘要 |
| 去重聚合 | 摘要前 30 字精确匹配 | 语义相似度聚类 |
| 上下文采集 | 模拟字段 | 真实埋点 + 性能监控 |
| 防刷 | 未实现 | 每日 5 次上限 |
| 权限 | 无 | 看板仅产品/负责人可访问 |
| 演示数据 | 内置 11 条，分享 HTML 即可演示 | 测试环境种子数据 |
| 钉钉通知 | Toast + 控制台模拟 | 钉钉开放平台工作通知 API |

---

## 12. 附录

### 12.1 模块树范围（一级）

- **产品中心**：基础信息、开发管理、任务管理、风控管理、产品管理、ECN 管理、智能选品
- **营销中心**：驾驶舱、listing 管理、广告管理、算价管理、刊登管理、运营管理等

完整三级菜单见原型 `MODULE_TREE` 配置。

### 12.2 相关文档

- 交互原型：`不爽即录.html`
- 原始需求：`_docx_extracted.txt`
- 使用说明：`README.md`

---

**文档结束**
